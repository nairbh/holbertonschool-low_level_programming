#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
#include <stdio.h>

int main(void) {
char  array[]="/"Programming is like building a multilingual puzzle;
puts(array);
return (0);
}
