#include <stdio.h>
#include "main.h"
#include <string.h>
#include <stdlib.h>

int _atoi(char *s)

{
	int signe = 1, i = 0,
	
	    while(*s < '0' || *s > '9')

	    {
		    if (*s++ == - 1)
		    {
			    signe = -1;
		    }

}
