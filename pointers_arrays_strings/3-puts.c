#include <stdio.h>
#include "main.h"
/**
 * Write a function that prints a string, followed by a new line, to stdout
 *
 * @:  _puts
 */
void _puts(char *str)
{
	char sdout[] = "String";

	return (sdout);
}
