#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - print out the absolute value of number
 * @b: number
 * Return: int absolute value
 */
int _abs(int l)
{
	int b;

	b = abs(l);

	return (b);
}
