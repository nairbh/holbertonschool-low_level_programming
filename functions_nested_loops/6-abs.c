#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - Computes the absolute value
 * of an integer.
 *
 * @l: input number as an integer.
 *
 * Return: absolute value
 */
int _abs(int l)
{
	int b;

	b = abs(l);

	return (b);
}
